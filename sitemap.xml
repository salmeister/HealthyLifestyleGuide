---
layout: null
---
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9"
        xmlns:image="http://www.google.com/schemas/sitemap-image/1.1"
        xmlns:video="http://www.google.com/schemas/sitemap-video/1.1">
  <!-- Homepage - Highest Priority -->
  <url>
    <loc>{{ site.url | replace: "www.", "" }}/</loc>
    <lastmod>{{ site.time | date: "%Y-%m-%d" }}</lastmod>
    <changefreq>daily</changefreq>
    <priority>1.0</priority>
  </url>
  
  <!-- About Page -->
  <url>
    <loc>{{ site.url | replace: "www.", "" }}/about/</loc>
    <lastmod>{{ site.time | date: "%Y-%m-%d" }}</lastmod>
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
  </url>
  

  
  <!-- Notes/Articles - High Priority Content -->
  {% for note in site.notes %}
  <url>
    <loc>{{ note.url | absolute_url | replace: "www.", "" }}</loc>
    {% if note.lastmod %}
    <lastmod>{{ note.lastmod | date: "%Y-%m-%d" }}</lastmod>
    {% elsif note.date %}
    <lastmod>{{ note.date | date: "%Y-%m-%d" }}</lastmod>
    {% else %}
    <lastmod>{{ site.time | date: "%Y-%m-%d" }}</lastmod>
    {% endif %}
    <changefreq>monthly</changefreq>
    <priority>0.8</priority>
    {% if note.image %}
    <image:image>
      <image:loc>{{ note.image | absolute_url }}</image:loc>
      <image:title>{{ note.title | xml_escape }}</image:title>
      <image:caption>{{ note.description | xml_escape | default: note.title | xml_escape }}</image:caption>
    </image:image>
    {% endif %}
    {% assign link_url = note.link | downcase %}
    {% if link_url contains 'youtube.com' or link_url contains 'youtu.be' %}
    {% if note.link contains 'v=' %}
    {% assign vid_id = note.link | split: 'v=' | last | split: '&' | first %}
    {% elsif note.link contains 'youtu.be/' %}
    {% assign vid_id = note.link | split: 'youtu.be/' | last | split: '?' | first %}
    {% endif %}
    {% if vid_id %}
    <image:image>
      <image:loc>https://img.youtube.com/vi/{{ vid_id }}/maxresdefault.jpg</image:loc>
      <image:title>{{ note.title | xml_escape }}</image:title>
      <image:caption>{{ note.description | xml_escape | default: note.title | xml_escape }}</image:caption>
    </image:image>
    {% endif %}
    {% endif %}
  </url>
  {% endfor %}
  
  <!-- Static Pages -->
  {% for page in site.pages %}
    {% unless page.url contains '/404' or page.url == '/' or page.url contains 'sitemap' or page.url contains 'robots' or page.url contains 'feed' or page.url contains '.json' %}
    <url>
      <loc>{{ page.url | absolute_url | replace: "www.", "" }}</loc>
      <lastmod>{{ site.time | date: "%Y-%m-%d" }}</lastmod>
      <changefreq>monthly</changefreq>
      <priority>0.6</priority>
    </url>
    {% endunless %}
  {% endfor %}
</urlset>